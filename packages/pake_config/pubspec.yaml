name: pake_config
description: Shared configuration model for pake_mobile — written by pake_cli, read by pake_shell.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dev_dependencies:
  test: ^1.25.0
